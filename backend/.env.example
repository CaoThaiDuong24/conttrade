# Backend Environment Variables (Fastify)

# Server Configuration
NODE_ENV=production
PORT=3006
HOST=0.0.0.0

# Database (use 'postgres' service name in Docker, or 'localhost' for local development)
DATABASE_URL=postgresql://postgres:password@postgres:5432/i_contexchange?schema=public

# JWT Secret
JWT_SECRET=your-jwt-secret-key-change-in-production

# CORS
CORS_ORIGIN=http://localhost:3000

# Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# Session
SESSION_SECRET=your-session-secret-change-in-production

# ============================================================
# EMAIL SERVICE (SendGrid) - For Rental Contract Notifications
# ============================================================
# Get your API key from: https://app.sendgrid.com/settings/api_keys
# Free tier: 100 emails/day (enough for testing)
# Paid tier: $20/month for 50,000 emails
SENDGRID_API_KEY=your_sendgrid_api_key_here

# Email From Address (must be verified in SendGrid)
# Format: email@yourdomain.com
EMAIL_FROM=noreply@conttrade.com

# Email From Name (displayed in recipient's inbox)
EMAIL_FROM_NAME=ContTrade Platform

# ============================================================
# OPTIONAL: Email Settings
# ============================================================
# Enable/disable email sending (true/false)
# Set to 'false' for development to prevent accidental email sends
EMAIL_ENABLED=true

# Email retry attempts on failure
EMAIL_RETRY_ATTEMPTS=3

