# Backend Environment Variables (Fastify)

# Server Configuration
NODE_ENV=production
PORT=3006
HOST=0.0.0.0

# Database (use 'postgres' service name in Docker, or 'localhost' for local development)
DATABASE_URL=postgresql://postgres:password@postgres:5432/i_contexchange?schema=public

# JWT Secret
JWT_SECRET=your-jwt-secret-key-change-in-production

# CORS
CORS_ORIGIN=http://localhost:3000

# Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# Session
SESSION_SECRET=your-session-secret-change-in-production
